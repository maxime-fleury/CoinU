# Freebuff desktop app metadata (local workspace state)
.freebuff
desktop-v2.db*


# dependencies (bun install)
node_modules

# output
out
*.tgz

# dist/main.js is committed on purpose: the GitHub Pages site is served from
# the repository branch (index.html hard-references dist/main.js), so the
# bundle must be part of the repo for the live demo to work.

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
